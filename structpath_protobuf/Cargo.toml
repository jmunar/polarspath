[package]
name = "structpath_protobuf"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "Automatic structpath implementations for Protocol Buffer messages"
repository = "https://github.com/jmunar/polarspath"
keywords = ["structpath", "protobuf", "protocol-buffers", "polars", "prost"]
categories = ["data-structures", "encoding"]

[features]
default = []
build = ["prost-build", "prost-types"]

[dependencies]
polars-arrow = { workspace = true }
polars-core = { workspace = true, features = ["dtype-array", "dtype-struct"] }
prost = { workspace = true }
structpath = { workspace = true, features = ["derive"] }
prost-build = { workspace = true, optional = true }
prost-types = { workspace = true, optional = true }
